#import "BlinkController.hpp"

namespace {
    const int WaitTime = 1000;
    // Project::LEDController ledController(LED_BUILTIN);
}

void setup() {
  // ledController.Setup();  
}

void loop() {
  // ledController.Toggle();
  // wait(WaitTime);
}